📰 News Parser Bot

Автоматизований Telegram-бот для збору та відправлення актуальних новин з обраних джерел. News Parser Bot автоматично моніторить новинні ресурси та надсилає найважливіші події користувачам. Бот підтримує категоризацію новин, інтеграцію з AI для генерації контенту та зручний інтерфейс взаємодії.

✨ Можливості

🔄 Автоматичний парсинг новин з обраних RSS-джерел та веб-сайтів

🤖 AI-генерація контенту для обробки та узагальнення новин

📱 Зручний Telegram інтерфейс з підтримкою команд

🎯 Фільтрація за категоріями: IT, спорт, політика, економіка

⚡ Keep-alive механізм для безперервної роботи

📊 Валідація URL та обробка зображень з новин

🏗️ Технології

Python 3.8+

python-telegram-bot — робота з Telegram Bot API

requests — HTTP запити для отримання новин

BeautifulSoup4 — парсинг HTML контенту

Flask — веб-сервер для keep-alive функціоналу

📁 Структура проєкту
tg_bot/
├── run.py              # Головний файл запуску бота
├── handlers.py         # Обробники команд та повідомлень користувачів
├── ai_generate.py      # Модуль для генерації контенту за допомогою AI
├── news_scraper.py     # Парсер новин з RSS фідів та веб-джерел
├── config.py           # Конфігураційні налаштування та змінні
├── keep_alive.py       # Flask сервер для підтримки активності бота
├── Procfile            # Конфігурація для запуску процесів
└── pon.txt             # Додаткові дані

📝 Опис модулів

run.py — головний файл, який ініціалізує та запускає бота, підключає всі модулі та обробники команд.

handlers.py — містить обробники всіх команд користувача (/start, /help, /news тощо) та логіку взаємодії з користувачем.

ai_generate.py — модуль для роботи з AI API, генерує та обробляє контент, створює короткі описи новин або відповіді на запити користувачів.

news_scraper.py — збирає новини з різних джерел (RSS фіди, веб-сайти), парсить HTML, валідує URL та обробляє зображення.

config.py — зберігає конфігураційні параметри: токени, API ключі, джерела новин, інтервали оновлення.

keep_alive.py — запускає простий Flask веб-сервер для підтримки бота в активному стані.

🚀 Встановлення

Клонування репозиторію:

git clone https://github.com/daniyil64345/tg_bot.git
cd tg_bot


Встановлення залежностей:

pip install -r requirements.txt


Налаштування: створіть файл .env у кореневій директорії:

TELEGRAM_TOKEN=your_telegram_bot_token
API_KEY=your_ai_api_key


Як отримати токен: знайдіть @BotFather у Telegram, надішліть /newbot та дотримуйтесь інструкцій, потім скопіюйте токен у файл .env.

Запуск бота:

python run.py
